server:
  port: 8082 # Port for the Loan Service

spring:
  application:
    name: loan-service # Name of the service to register with Eureka

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka # URL of the Eureka Server
