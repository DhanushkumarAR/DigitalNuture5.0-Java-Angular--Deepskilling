server:
  port: 8761 # Default port for Eureka Server

eureka:
  client:
    register-with-eureka: false # Eureka server itself does not need to register with itself
    fetch-registry: false      # Eureka server does not need to fetch registry from itself
  instance:
    hostname: localhost
